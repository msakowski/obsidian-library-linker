---
'jw-library-linker': minor
---

Offline Bible settings now show a list of all installed languages with per-language Remove buttons, and the Import button is always available — the language is detected from the EPUB metadata. Importing an EPUB no longer requires switching the plugin's main language first. Re-importing a Bible for a language that is already installed shows a notice asking you to remove it first.
