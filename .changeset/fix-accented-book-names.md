---
"jw-library-linker": patch
---

Fix Bible reference detection for book names with accented characters (e.g., Génesis, Éxodo, Levítico) by using Unicode letter matching instead of ASCII-only character classes
