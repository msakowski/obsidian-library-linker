export const bibleReferenceRegex =
  /(?:[1-5]?\.?\s?[A-Za-zäöü ]{2,24}\.?\s?\d+:\d+(?:-\d+)?(?:\s*,\s*\d+(?:-\d+)?)*\s*,?\s*)/gi;
// 24 random number. Apostelgeschichte is 17 characters long.
// should be enough for language support.
